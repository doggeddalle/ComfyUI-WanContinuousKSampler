[project]
name = "comfyui-wan-continuous-ksampler"
version = "0.4.0"
description = "Wan 2.2 sampling and consolidated SCAIL-2 inference tools for ComfyUI."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
license = { file = "LICENSE" }
classifiers = [
    "License :: OSI Approved :: Apache Software License",
]

[tool.comfy]
PublisherId = "local"
DisplayName = "Wan Sampler Toolkit"

[tool.unittest]
test-pattern = "test*.py"
